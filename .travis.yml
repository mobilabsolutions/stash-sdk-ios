language: objective-c
osx_image: xcode10.1

cache:
  directories:
  - Carthage

env:
  #environment variables
  global:
    - PROJECT_NAME="MobilabPayment.xcodeproj"
  matrix:
    - DESTINATION="OS=12.1,name=iPhone X"

before_install:
  - brew install carthage || true
  - brew outdated carthage || brew upgrade carthage

before_script:
  - carthage update --cache-builds --platform iOS

script:
  - set -e
  - echo "Starting Unit Tests"
  - travis_retry xcodebuild clean test -quiet -project "$PROJECT_NAME" -scheme "MobilabPaymentCore" -destination "$DESTINATION" test
  - echo "Starting UI Tests"
  - travis_retry xcodebuild test -quiet -project "$PROJECT_NAME" -scheme "Demo" -destination "$DESTINATION" test
  - echo "Building Objective-C Demo"
  - xcodebuild test -quiet -project "$PROJECT_NAME" -scheme "Demo-ObjC" -destination "$DESTINATION" 
